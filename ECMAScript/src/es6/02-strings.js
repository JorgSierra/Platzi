let hello = 'Hello';
let world = 'World';
let epicPhrase = hello + " " + world + '!';
console.log(epicPhrase);

// Template literals
let epicPhrase2 = `${hello} ${world}!`;
console.log(epicPhrase2);

// Multi-line
let phrase = 'esto es un string \n' + 'esta es otra linea';
let phrase2 = `Esta es una frase
con dos lineas`;

console.log(phrase);
console.log(phrase2);